---
phase: 03-memory-integrity-and-data-lifecycle
plan: 06
type: execute
wave: 4
depends_on: [03-04, 03-05]
files_modified:
  - aura_backend/main.py
  - aura_backend/conversation_persistence_service.py
  - aura_backend/aura_internal_tools.py
  - aura_backend/runtime/config.py
  - aura_backend/storage/cli.py
  - tests/api/test_api_compatibility.py
  - tests/runtime/test_base_install_startup.py
  - tests/storage/test_storage_owner.py
autonomous: true
requirements: [PRES-04, TEST-03, DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, DATA-06, DATA-09]
must_haves:
  truths:
    - "The active conversation path commits a complete idempotent SQLite turn first, then reconciles Chroma; no active path writes durable truth to Chroma or uses live-directory backup."
    - "The seven-field conversation response, HTTP-200 typed provider fallback, answer-preserving degraded persistence behavior, loopback/no-sign-in boundary, and untrusted memory delimiters remain exact."
    - "Search/history are bounded neutral storage-boundary calls with stable cursors/traces; export and deletion use the lifecycle service rather than nonfunctional/direct collection operations."
    - "Historical reads stay on an explicit legacy-read adapter until the later authorized switch; new durable writes still have only one SQLite owner."
    - "A content-free CLI can validate, benchmark, snapshot/restore, import from authorized disposable copies, switch/rollback reads, and validate evidence without installing/syncing or inferring destructive intent."
  artifacts:
    - path: "aura_backend/conversation_persistence_service.py"
      provides: "Compatibility service over atomic ledger and post-commit projection"
    - path: "aura_backend/main.py"
      provides: "Injected storage boundary for conversation/search/history/export/deletion routes"
    - path: "aura_backend/storage/cli.py"
      provides: "Fail-closed operational boundary for later human-gated evidence actions"
    - path: "tests/api/test_api_compatibility.py"
      provides: "End-to-end stable response, persistence, retrieval, lifecycle, and local-boundary proof"
  key_links:
    - from: "aura_backend/main.py"
      to: "aura_backend/conversation_persistence_service.py"
      via: "lifespan-injected repository/projection and one stable idempotency key across retry"
      pattern: "idempotency_key|persist_conversation_exchange"
    - from: "aura_backend/main.py"
      to: "aura_backend/storage/retrieval.py"
      via: "bounded search/history through selected read owner"
      pattern: "next_cursor|has_more|trace"
    - from: "aura_backend/main.py"
      to: "aura_backend/storage/lifecycle.py"
      via: "real JSON export and explicit deletion protocol"
      pattern: "export_scope_json|deletion_plan|confirm"
---

<objective>
Route Aura's stable runtime and API through the new storage owner while preserving every Phase 1/2 public and local-runtime contract.

Purpose: Make the new ledger/retrieval/lifecycle system real application behavior without silently reading/migrating historical data or destabilizing provider/runtime work.
Output: SQLite-first persistence, bounded read adapters, lifecycle endpoints, operational CLI, and full compatibility tests.
</objective>

## Artifacts this plan produces

- One lifespan-owned storage composition with explicit absolute v2 data/projection paths and no import-time database/model work.
- A compatibility persistence service whose internal outcomes are typed while its observed Phase 2 behavior remains stable.
- Bounded search/history/export/deletion API contracts and provider-neutral internal memory tools.
- Operational commands consumed only by the non-autonomous real-data plans that follow.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-memory-integrity-and-data-lifecycle/03-CONTEXT.md
@.planning/phases/03-memory-integrity-and-data-lifecycle/03-RESEARCH.md
@.planning/phases/02-provider-and-runtime-core/02-PATTERNS.md
@.planning/phases/02-provider-and-runtime-core/02-VERIFICATION.md
@aura_backend/main.py
@aura_backend/conversation_persistence_service.py
@aura_backend/aura_internal_tools.py
@aura_backend/runtime/app.py
@aura_backend/runtime/config.py
@tests/api/test_provider_compatibility.py
@tests/characterization/test_companion_contract.py
@tests/characterization/test_persistence_contract.py
@tests/api/test_local_boundary.py
</context>

<dependencies>
Needs: Complete ledger/projection/retrieval/lifecycle behavior from Plans 03-01 through 03-05.
Creates: Stable application and command seams needed before fresh preservation and real-data authorization.
Wave rationale: All code slices converge here, so this plan waits for both Wave 3 plans.
</dependencies>

<scope_fence>
- Implement D-01 through D-09 at the runtime boundary while preserving the exact seven response fields, HTTP-200 provider fallback, answer-preserving degraded storage path, prompt memory delimiters, loopback default, explicit origins, and no mandatory sign-in.
- The default for an existing historical installation remains explicit legacy reads until Plan 03-09 writes a verified SQLite read-owner marker. There is no dual durable write: all new writes go to SQLite, and Chroma receives only rebuildable post-commit projections.
- Do not open/import/mutate/delete real historical roots while executing this plan; runtime/API tests inject temporary legacy/new roots and prohibit repository-data effects.
- Do not add auth, affect, relationship updates, graph/RLM, frontend redesign, source-data deletion, dependency upgrades, manifests/locks, at-rest encryption claims, or Git-history work.
</scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-06-01: Wire SQLite-first turn persistence without public drift</name>
  <files>aura_backend/main.py, aura_backend/conversation_persistence_service.py, aura_backend/runtime/config.py, tests/api/test_api_compatibility.py, tests/runtime/test_base_install_startup.py</files>
  <read_first>
    @aura_backend/main.py
    @aura_backend/conversation_persistence_service.py
    @aura_backend/runtime/config.py
    @aura_backend/runtime/app.py
    @aura_backend/storage/repository.py
    @aura_backend/storage/projection.py
    @tests/api/test_provider_compatibility.py
    @tests/characterization/test_persistence_contract.py
  </read_first>
  <behavior>
    - Runtime settings parse an explicit absolute `AURA_LEDGER_DIRECTORY` override and a repository-resolved ignored `aura_data_v2` default without changing the legacy `AURA_DATA_DIRECTORY`; malformed/overlapping historical paths fail before resource construction.
    - Lifespan constructs one repository, projection, retriever, lifecycle service, and read-owner selector through injected ordinary objects; import remains resource-free and partial startup closes in reverse order.
    - `ConversationRequest` accepts an optional stable idempotency key without making old clients invalid; when absent, one opaque key is generated once before persistence and reused by the background retry.
    - `ConversationPersistenceService` converts one normalized exchange into one atomic turn command, returns the existing compatibility result keys plus typed status/retry identity, commits before projection, and retries/reconciles without duplicating turns.
    - A projection failure degrades storage status while preserving the provider answer and scheduling one bounded reconciliation attempt; provider failure still returns the exact seven-field HTTP-200 fallback, clears one session, and performs zero persistence.
    - Profile mutation is owned by the repository transaction/profile contract; required base startup no longer starts `DatabaseProtectionService` or treats `RobustAuraVectorDB` as durable truth.
  </behavior>
  <action>Apply the complete tracer cycle. RED: extend route/startup compatibility tests with same-key retry, changed-hash conflict, post-commit projection failure/reconcile, partial startup cleanup, no legacy writer/backup construction, exact response-key/fallback/degraded-answer assertions, and repository-root effect traps. GREEN: add explicit ledger settings, construct storage resources inside lifespan, and refactor `ConversationPersistenceService` over `StorageRepository` plus `ProjectionAdapter` while retaining its characterized async method names/result shape. Generate/reuse one idempotency key per exchange and keep projection strictly post-commit. REFACTOR only while the new API tests plus existing provider/companion/persistence/startup suites stay green. Do not add a response field or call the provider twice on persistence retry.</action>
  <acceptance_criteria>
    - New turns and retries converge in SQLite and projection failures never create dual truth.
    - Conversation success/fallback/degraded storage preserve exact Phase 1/2 observable behavior and seven keys.
    - Base import/lifespan remains local, no-sign-in, resource-owned, and partial-start safe.
    - No test opens or changes a real historical root.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/api/test_api_compatibility.py tests/api/test_provider_compatibility.py tests/characterization/test_companion_contract.py tests/characterization/test_persistence_contract.py tests/runtime/test_base_install_startup.py</automated>
  </verify>
  <done>The active write path has one atomic SQLite truth owner and retains the complete public/provider compatibility contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-06-02: Route reads, export, deletion, and operations through the storage boundary</name>
  <files>aura_backend/main.py, aura_backend/aura_internal_tools.py, aura_backend/storage/cli.py, tests/api/test_api_compatibility.py, tests/storage/test_storage_owner.py</files>
  <read_first>
    @aura_backend/main.py
    @aura_backend/aura_internal_tools.py
    @aura_backend/storage/retrieval.py
    @aura_backend/storage/lifecycle.py
    @aura_backend/storage/migration.py
    @aura_backend/storage/benchmark.py
    @.github/workflows/ci.yml
    @tests/api/test_filesystem_contract.py
    @tests/memory/test_prompt_memory_boundary.py
  </read_first>
  <behavior>
    - Read-owner selection is explicit and content-free: historical installations remain `legacy` until a verified marker says `sqlite`; an empty clean install can use SQLite without manufacturing imported history.
    - `/search` and internal `aura.search_memories` use the selected boundary, fixed candidate/page bounds, and return compatibility result fields plus `next_cursor`, `has_more`, and trace ID; Memvid search is never the primary eligible set.
    - Chat history/session messages use bounded keyset cursors with default 20/hard max 100 while retaining existing session/message fields.
    - `/export/{user_id}` invokes actual JSON lifecycle export and rejects every unsupported format before writing.
    - Deletion endpoints expose exact plan, confirm, execute, and verify operations. The legacy DELETE route with no valid confirmation performs no mutation and returns an explicit confirmation-required plan result.
    - CLI commands cover `benchmark`, `snapshot-verify`, `record-authorization`, `migrate-authorized-copy`, `switch-reads`, `rollback-reads`, `prepare-ci-publication`, `verify-ci-publication`, `collect-ci-evidence`, and `validate-evidence`; each requires explicit paths/evidence, bounded resources, fixed safe exits, and never installs/syncs/deletes inferred targets.
    - `record-authorization` accepts only an exact approved checkpoint result, writes private/public canonical JSON with exclusive creation, binds source/restore/plan/scope/time/expiry/result/digests, and never exposes absolute historical paths or content publicly.
    - CI evidence parsing binds a clean current `HEAD` SHA and current workflow digest to completed-success `deterministic-backend`, `lint`, `typing-python`, `typing-frontend`, and `frontend-build`; missing, old-SHA, pending, failed, cancelled, skipped, not-run, or resource-limited results are non-pass, while live Ornith/model lanes are not required.
    - CI publication preparation is content-free and mutation-free, fixes remote `origin`, preferred branch `phase-03-memory-integrity-ci`, optional `main` only on explicit human selection, exact clean implementation SHA, workflow path/digest, expiry, and read-only pre-action remote heads. Publication verification rejects force/non-fast-forward main rewrite/tag/release/unnamed-ref scope, different remote SHA, stale/wrong run, or workflow mismatch.
    - Stored memory remains inside untrusted delimiters, and selected/retrieved content never writes new evidence or system/tool authority.
  </behavior>
  <action>Use RED, complete GREEN, then conditional REFACTOR. RED: specify bounded search/history cursors, cross-scope/stale/injection rejection, real JSON endpoint contents, unsupported formats, vague/confirmed/replayed/partial deletion, legacy-read selection, SQLite marker selection/rollback, CLI path/evidence/resource failure, exclusive authorization receipt creation/binding, local-only CI publication proposal/read-only verification, current-SHA CI job parsing, and exact local/no-auth behavior. GREEN: route `main.py` and `AuraInternalTools` through injected storage interfaces, replace nonfunctional/direct Chroma lifecycle calls, and implement a standard-library CLI with lazy imports and content-free JSON summaries. Receipt commands reject missing/non-approved/stale/widened/replayed input. CI publication preparation writes only outside-Git evidence and never invokes push/dispatch; verification performs read-only remote/run checks for the exact safe branch/SHA/workflow. CI collection requires the five deterministic jobs at completed-success, classifies unavailable evidence as environment-blocked/non-pass, and never lets historical Phase 2 or live/model lanes substitute. Keep legacy reads as an explicit adapter only; it cannot accept writes or serve as truth after the marker switch. REFACTOR only while API, storage-owner, prompt-memory, filesystem, provider, runtime, and lifecycle tests remain green.</action>
  <acceptance_criteria>
    - Search/history are bounded, neutral, traceable, stable, and scope-safe.
    - Export/deletion call the lifecycle owner and cannot claim incomplete/partial success.
    - Internal tools no longer bypass the owner or promote optional Memvid into core retrieval.
    - Operational commands fail closed and require later checkpoint evidence for real import/switch.
    - Authorization and CI evidence commands are canonical, exact-scope/current-SHA, privacy-safe, never auto-push/dispatch, and adversarially fail closed.
    - Full Phase 2 runtime/provider behavior remains green.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/api/test_api_compatibility.py tests/storage/test_storage_owner.py tests/storage/test_hybrid_retrieval.py tests/storage/test_export_delete.py tests/storage/test_snapshot_restore.py tests/memory/test_prompt_memory_boundary.py tests/api/test_filesystem_contract.py tests/api/test_local_boundary.py tests/runtime</automated>
  </verify>
  <done>All persistence, retrieval, export, deletion, and operational paths cross one injected storage boundary while legacy reads remain explicitly gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Loopback API → storage owner | Caller content, scope, cursor, format, and destructive intent enter durable/local operations. |
| Provider result → atomic turn | A visible answer and its persistence/projection status must remain distinguishable. |
| Legacy read adapter → new runtime | Historical state is untrusted/read-only until approved import and switch. |
| CLI evidence/path → real-data operation | Commands must not infer authorization or escape approved roots. |
| Human/CI evidence → authorization/acceptance | Narrow human scope and current-revision green jobs must be tamper-evident and non-substitutable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-06-01 | Tampering / Repudiation | route persistence/retry | mitigate | One scoped idempotency key/hash, atomic repository, post-commit projection, compatibility tests. |
| T-03-06-02 | Information Disclosure / Elevation | search/history/cursors | mitigate | Selected owner, hard scope gates, bounds, integrity-bound cursors, two-scope tests. |
| T-03-06-03 | Spoofing / Elevation | recalled instructions | mitigate | Existing untrusted delimiters and no evidence/tool/system promotion. |
| T-03-06-04 | Tampering / Denial of Service | lifecycle API/CLI | mitigate | Strict enums/formats, plan-confirm-verify, explicit evidence/path requirements, fixed bounds/exits. |
| T-03-06-05 | Information Disclosure | diagnostics | mitigate | Safe codes/IDs/counts/timings only; sentinel log/response tests. |
| T-03-06-06 | Spoofing / Repudiation | authorization and CI evidence | mitigate | Canonical digests, exclusive receipts, exact approval/scope/expiry, clean HEAD and required-job parser. |
| T-03-06-SC | Tampering | package supply chain | mitigate | No manifests/locks/installs/syncs/new dependency; standard library CLI. |
</threat_model>

<verification>
- Capture each task's expected RED and exact GREEN run.
- Run `uv run --locked --no-sync python -m pytest -q tests/api/test_api_compatibility.py tests/api/test_provider_compatibility.py tests/characterization/test_companion_contract.py tests/characterization/test_persistence_contract.py tests/api/test_local_boundary.py tests/runtime tests/storage tests/memory`.
- Run the complete contract: `uv run --locked --no-sync python -m pytest tests -q -m 'not live'`.
- Confirm no manifest/lock, real data root, auth boundary, response key set, prompt delimiter, or optional Memvid copy-only behavior changed.
</verification>

<source_coverage>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Coverage | Status |
|---|---|---|---|---|
| GOAL | — | One truthful runtime storage boundary | Both tasks | COVERED |
| REQ | DATA-01 | One owner across persistence/index/lifecycle | Lifespan composition and storage-owner tests | COVERED |
| REQ | DATA-02 | Backup/restore boundary remains routed through one owner | Task 02 API/CLI wiring | COVERED |
| REQ | DATA-03 | Real JSON export | Task 02 endpoint wiring | COVERED |
| REQ | DATA-04 | Explicit complete deletion | Task 02 endpoints | COVERED |
| REQ | DATA-05 | Bounded measured retrieval/history | Task 02 | COVERED |
| REQ | DATA-06 | Atomic events/provenance | Task 01 write adapter | COVERED |
| REQ | DATA-09 | Neutral eligible set/trace | Task 02 read adapter | COVERED |
| REQ | TEST-03 | API/persistence/failure tests | Both tasks and full regression | COVERED |
| REQ | PRES-04 | Generated paths remain untracked | Explicit v2 root plus prior hygiene gate | COVERED |
| CONTEXT | D-01–D-09 | Locked storage/retrieval/lifecycle/evaluation/local contracts | Both tasks | COVERED |
| RESEARCH | Staged writes, legacy reads, gated switch, no dual durable writes | Both tasks | COVERED |

Actual real-root inventory/backup/import/read-switch remains Plans 03-07 through 03-09. Affect, graph/RLM, frontend, source deletion/repair, broad upgrades, encryption claims, auth, and Git-history work are excluded and are not gaps.
</source_coverage>

<success_criteria>
- Exact compatibility, storage, lifecycle, retrieval, prompt, local-boundary, and full non-live suites pass.
- Required base runtime uses one SQLite truth owner and no legacy Chroma/database-protection writer.
- No real historical root is opened or changed before the checkpoint plans.
</success_criteria>

<output>
Create `.planning/phases/03-memory-integrity-and-data-lifecycle/03-06-SUMMARY.md` when done.
</output>
