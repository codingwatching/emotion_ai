---
phase: 03-memory-integrity-and-data-lifecycle
plan: 09
type: execute
wave: 7
depends_on: [03-08]
files_modified:
  - .planning/evidence/phase-03/import-summary.json
  - .planning/evidence/phase-03/benchmark-summary.json
  - .planning/evidence/phase-03/snapshot-restore-summary.json
  - .planning/evidence/phase-03/read-switch-summary.json
  - tests/storage/test_phase3_evidence.py
autonomous: false
requirements: [PRES-04, TEST-03, DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, DATA-06, DATA-09]
must_haves:
  truths:
    - "Only Ty-authorized disposable restored copies are read; the originals, durable backup, archives, and Phase 1 evidence remain byte-identical."
    - "Migration preflight accepts only the current immutable Plan 03-08 receipt with exact approved sources/fingerprints, digests, scope, result, private-summary digest, and unexpired unused authorization ID."
    - "Both historical Chroma roots are imported once into SQLite with separate source identities, exact counts/digests, preserved duplicates/fragments/anomalies, complete provenance, and no cross-scope leakage."
    - "The Chroma cosine projection is rebuilt solely from SQLite and passes integrity plus neutral retrieval competence, failure-control, and salience-zero gates before reads can switch."
    - "A new SQLite online snapshot restores exactly in isolation and rollback to legacy reads is rehearsed before the single atomic read-owner marker can select SQLite."
    - "The seven-field response contract, HTTP-200 degraded provider behavior, loopback/no-sign-in boundary, prompt memory delimiters, Memvid copy-only rule, and complete Phase 2 non-live suite still pass after the switch."
    - "After the implementation commit is clean and fixed, only Ty may publish that exact SHA to the preferred `phase-03-memory-integrity-ci` branch, or explicitly choose a normal fast-forward `main` push/workflow dispatch; no task auto-pushes or dispatches."
    - "A human can inspect content-free evidence for import, benchmark, snapshot/restore, rollback, and read ownership before accepting Phase 3."
    - "Before final acceptance, the clean current implementation commit SHA—not historical Phase 2—has completed-success CI evidence for deterministic backend tests, lint, Python typing, frontend typing, and frontend build; live model/Ornith lanes are not required."
  artifacts:
    - path: ".planning/evidence/phase-03/import-summary.json"
      provides: "Sanitized exact two-root import/provenance/projection proof"
    - path: ".planning/evidence/phase-03/benchmark-summary.json"
      provides: "Sanitized controls-first neutral retrieval competence report"
    - path: ".planning/evidence/phase-03/snapshot-restore-summary.json"
      provides: "Sanitized SQLite online-snapshot and exact isolated-restore proof"
    - path: ".planning/evidence/phase-03/read-switch-summary.json"
      provides: "Sanitized rollback rehearsal, compatibility suite, and atomic owner-marker result"
    - path: "tests/storage/test_phase3_evidence.py"
      provides: "Adversarial verifier for all migration/read-switch evidence and privacy allowlists"
    - path: "/backup/aura-phase-03/phase-03-storage-gate-01/ci/current-revision-ci.json"
      provides: "Outside-Git public-safe exact-HEAD required-job CI evidence used by the final acceptance gate"
    - path: "/backup/aura-phase-03/phase-03-storage-gate-01/ci/ci-publication-proposal.json"
      provides: "Public-safe exact clean-SHA/explicitly selected remote-ref/workflow human-action proposal created without network mutation"
    - path: "/backup/aura-phase-03/phase-03-storage-gate-01/ci/ci-publication-receipt.json"
      provides: "Public-safe read-only proof that the explicitly selected remote ref and fresh GitHub run bind the human-published exact SHA/workflow"
  key_links:
    - from: ".planning/evidence/phase-03/migration-authorization-receipt.json"
      to: ".planning/evidence/phase-03/import-summary.json"
      via: "single-use authorization ID, exact alias/fingerprint set, restore/plan/summary digests, fixed scope/result, and expiry"
      pattern: "authorization_id|authorization_receipt_sha256|authorization_summary_sha256|approval_result"
    - from: ".planning/evidence/phase-03/migration-authorization-receipt.json"
      to: "/backup/aura-phase-03/phase-03-storage-gate-01/authorizations/migration-authorization-summary.private.json"
      via: "recomputed canonical private-summary digest and exact approved path/scope comparison"
      pattern: "authorization_summary_sha256|scope|source_alias"
    - from: ".planning/evidence/phase-03/import-summary.json"
      to: ".planning/evidence/phase-03/restore-drill-summary.json"
      via: "authorization, restore, source-alias/fingerprint, source-before/source-after, and private-log digests"
      pattern: "authorization_summary_sha256|restore_summary_sha256|source_alias|source_after_sha256"
    - from: ".planning/evidence/phase-03/benchmark-summary.json"
      to: ".planning/evidence/phase-03/import-summary.json"
      via: "ledger/projection fingerprints and predeclared control/competence gate set"
      pattern: "ledger_fingerprint|projection_fingerprint|control_gate|competence_gate"
    - from: ".planning/evidence/phase-03/read-switch-summary.json"
      to: "aura_data_v2/storage-owner.json"
      via: "atomic owner marker written only after import, benchmark, snapshot/restore, rollback, and compatibility gates pass"
      pattern: "owner_marker_sha256|read_owner|rollback_rehearsal|compatibility_suite"
    - from: "/backup/aura-phase-03/phase-03-storage-gate-01/ci/current-revision-ci.json"
      to: ".github/workflows/ci.yml"
      via: "clean current HEAD SHA, workflow digest, and completed-success results for all five deterministic required jobs"
      pattern: "commit_sha|workflow_sha256|required_jobs|conclusion"
    - from: "/backup/aura-phase-03/phase-03-storage-gate-01/ci/ci-publication-proposal.json"
      to: "/backup/aura-phase-03/phase-03-storage-gate-01/ci/ci-publication-receipt.json"
      via: "human-only preferred-safe-branch or explicitly selected fast-forward-main push/dispatch followed by read-only exact remote-ref/run/SHA/workflow verification"
      pattern: "implementation_sha|selected_remote_ref|human_selection|pre_action_remote_sha|workflow_path|workflow_sha256|run_id|head_sha"
    - from: "/backup/aura-phase-03/phase-03-storage-gate-01/ci/ci-publication-receipt.json"
      to: "/backup/aura-phase-03/phase-03-storage-gate-01/ci/current-revision-ci.json"
      via: "collector waits only on the verified fresh run for the exact implementation SHA"
      pattern: "publication_receipt_sha256|run_id|head_sha|required_jobs"
---

<objective>
Import the two historical stores from authorized disposable copies, prove every migration/retrieval/recovery/compatibility gate, and switch reads atomically to SQLite only on an all-green result.

Purpose: Complete the one-owner transition without risking or concealing damage to Ty's historical memory.
Output: SQLite truth ledger and rebuilt projection, four sanitized migration summaries, human-bound explicitly selected-ref publication proof, exact current-revision CI evidence outside Git, an atomic read-owner result, and final acceptance.
</objective>

## Artifacts this plan produces

- `aura_data_v2/memory.sqlite3` as the ignored SQLite source of truth and `aura_data_v2/chroma/` as its disposable cosine projection.
- `/backup/aura-phase-03/.migration-work/` as the mode-restricted disposable import workspace, never an original or durable backup path.
- `/backup/aura-phase-03/phase-03-storage-gate-01/snapshots/` as the immutable online SQLite snapshot and isolated restore drill.
- Private import/benchmark/snapshot/switch attempt logs outside Git and content-free public summaries in `.planning/evidence/phase-03/`.
- `/backup/aura-phase-03/phase-03-storage-gate-01/ci/current-revision-ci.private.json` plus public-safe `current-revision-ci.json`, both outside Git so recording CI proof does not change the proved commit SHA.
- Outside-Git public-safe CI publication proposal/receipt plus private raw verification, all bound to the exact human-selected `origin` ref (preferred `phase-03-memory-integrity-ci`; `main` only by explicit selection), clean implementation SHA, and `.github/workflows/ci.yml` digest.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-memory-integrity-and-data-lifecycle/03-CONTEXT.md
@.planning/phases/03-memory-integrity-and-data-lifecycle/03-RESEARCH.md
@.planning/phases/03-memory-integrity-and-data-lifecycle/03-08-SUMMARY.md
@.planning/evidence/phase-03/inventory-summary.json
@.planning/evidence/phase-03/restore-drill-summary.json
@.planning/evidence/phase-03/migration-plan-summary.json
@.planning/evidence/phase-03/migration-authorization-receipt.json
@.planning/evidence/phase-01/restore-drill-summary.json
@.planning/phases/02-provider-and-runtime-core/02-VERIFICATION.md
@.github/workflows/ci.yml
@aura_backend/storage/cli.py
@tests/storage/test_phase3_evidence.py
</context>

<dependencies>
Needs: Plan 03-08's current exact restore proof, content-free migration plan, and immutable unexpired single-use authorization receipt for disposable-copy read/import plus gated read switching.
Creates: Authorized one-time import, rebuilt projection, benchmark/recovery proof, human-bound exact-SHA publication, current-revision CI proof, and final SQLite read ownership.
Wave rationale: This is the only plan permitted to read historical content, and it must remain after the backup/restore and authorization wave.
Internal order: Task 03-09-02 requires Task 03-09-01's clean committed implementation SHA; Task 03-09-03 requires Task 03-09-02's passing publication receipt and exact fresh run ID.
</dependencies>

<scope_fence>
- Refuse to start unless the Plan 03-08 receipt is present, exactly approved, unexpired, unused, exact-scope, privacy-safe, and bound to the inventory, quiescence, backup, restore, migration-plan, source aliases/fingerprints, private authorization summary, and unchanged Phase 1 evidence.
- Never pass an original historical root or durable-backup root to Chroma. Make a new disposable migration child first, verify copy parity, and open only that child through the public Chroma API.
- Do not repair, deduplicate, summarize away, reinterpret, or delete historical rows. Preserve root identity, native IDs, duplicates, fragments, malformed/unknown dates, and known anomalies explicitly.
- Keep the legacy read owner on any failed, stale, omitted, truncated, vacuous, resource-limited, privacy, import, projection, benchmark, snapshot, restore, rollback, or compatibility gate. Never dual-write durable truth.
- Final Phase 3 acceptance additionally requires current clean-HEAD CI evidence. Missing, `not_run`, pending, old-SHA, failed, cancelled, skipped, or resource-limited required jobs are non-pass or explicit environment-blocked escalation, never success; historical Phase 2 and live/model lanes cannot substitute.
- No automatic task may push, dispatch, force-update, non-fast-forward rewrite `main`, create/update tags or releases, or mutate a remote ref. Publication is a blocking human action: `refs/heads/phase-03-memory-integrity-ci` is preferred, while `refs/heads/main` is allowed only by Ty's explicit normal fast-forward selection; all task-side remote checks are read-only.
- No source/backup/archive deletion, retention purge, tracked-data removal, Git-history mutation, authentication, graph/RLM, affect engine, frontend redesign, at-rest-encryption claim, or dependency change is authorized.
</scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-09-01: Import authorized copies and execute the fail-closed read-switch proof</name>
  <files>.planning/evidence/phase-03/import-summary.json, .planning/evidence/phase-03/benchmark-summary.json, .planning/evidence/phase-03/snapshot-restore-summary.json, .planning/evidence/phase-03/read-switch-summary.json, tests/storage/test_phase3_evidence.py</files>
  <read_first>
    @.planning/phases/03-memory-integrity-and-data-lifecycle/03-08-SUMMARY.md
    @.planning/evidence/phase-03/restore-drill-summary.json
    @.planning/evidence/phase-03/migration-plan-summary.json
    @.planning/evidence/phase-03/migration-authorization-receipt.json
    @/backup/aura-phase-03/phase-03-storage-gate-01/authorizations/migration-authorization-summary.private.json
    @.planning/evidence/phase-01/restore-drill-summary.json
    @aura_backend/storage/cli.py
    @aura_backend/storage/migration.py
    @aura_backend/storage/projection.py
    @aura_backend/storage/benchmark.py
    @tests/storage/test_phase3_evidence.py
  </read_first>
  <behavior>
    - Preflight rejects a missing/stale/tampered/widened/replayed/wrong-result receipt, expired or reused authorization ID, receipt/private-summary/restore/plan digest drift, unexpected roots, original/durable Chroma paths, nonempty target ledger, insufficient space, open writers, hash drift, or a reused disposable workspace; it reserves the authorization ID with exclusive creation before any copy/open so failures cannot replay it.
    - Each active historical root is copied again from the durable backup into a distinct disposable child, verified before/after, and imported exactly once with `(source_root_id, source_collection, source_record_id)` identity.
    - Import reports exact scanned/imported/duplicate/fragments/anomaly counts and table/provenance digests; no orphan provenance, cross-scope link, broken supersession chain, or active-row divergence is accepted.
    - Projection is cleared and rebuilt from active SQLite rows; exact eligible/projected/quarantined parity, stable IDs, cosine metadata, and ledger/projection fingerprints are required.
    - The frozen sanitized benchmark runs controls before competence gates; FTS-only, vector-only, fused RRF, hard scope/provenance/supersession/staleness gates, stable traces/cursors, and exactly zero Phase 4 salience contribution all pass predeclared thresholds.
    - SQLite online backup, immutable destination, exact isolated restore, FK/integrity/count/digest parity, and disposable projection rebuild/retrieval proof all pass.
    - Rollback is rehearsed to legacy reads first. The atomic marker selects SQLite only after every prior gate, and any post-switch compatibility failure atomically restores legacy ownership and records a truthful non-pass result.
  </behavior>
  <action>Use an evidence tracer. RED: extend `test_phase3_evidence.py` with missing/stale/tampered/widened/replayed/wrong-result authorization receipts; expired/reused IDs; receipt-to-private-summary/restore/plan/source digest substitution; absent/incomplete/forged import/projection/benchmark/snapshot/restore/rollback/switch summaries; privacy leakage, content fields, source drift, path aliasing, count/digest mismatch, omitted controls, nonzero salience, premature switch, and claimed success after rollback. Add synthetic CI publication parser cases for dirty worktrees, absent/different implementation SHA, any force/non-fast-forward-main-rewrite/tag/release/unnamed-ref scope, unrecorded `main` selection, wrong remote/branch, stale run, wrong head SHA/event/workflow path/workflow digest, and automatic task network mutation. Also add synthetic CI result parser cases for missing/`not_run`/pending/old-SHA/failed/cancelled/skipped/resource-limited required jobs, wrong workflow digest, and improper reliance on historical Phase 2 or live/model lanes; these parser tests do not require external CI. GREEN: invoke `uv run --locked --no-sync python -m aura_backend.storage.cli migrate-authorized-copy` with the immutable Plan 03-08 authorization receipt, public migration plan, durable backup, unique `/backup/aura-phase-03/.migration-work` child, ignored `aura_data_v2` target, private outside-Git attempt directory, and public import summary. The command must reserve the authorization ID through an exclusive-create private consumption record before copying or opening any store; retain that record on every success/failure so replay fails. Run the CLI projection rebuild and frozen sanitized benchmark, requiring controls before competence thresholds and writing the benchmark summary. Run the CLI online snapshot/isolated restore into the approved outside-Git snapshot root and write the snapshot summary. Rehearse legacy rollback, atomically switch reads, run the full non-live suite, and on any failure invoke `rollback-reads` before recording `read-switch-summary.json` as non-pass. Finish with `validate-evidence` and the exact pytest verifier. Refactor test/evidence helpers only while green. Never open originals/durable backup with Chroma, mutate or delete retained data, expose content in public summaries, leave SQLite selected after a failed gate, or perform a remote push/dispatch.</action>
  <acceptance_criteria>
    - Both authorized aliases have exact immutable source-copy and one-time import evidence with anomalies preserved.
    - Receipt validation proves exact current scope/result/digests/expiry and one-time consumption; every missing/stale/tampered/widened/replayed/wrong-result case fails.
    - SQLite/projection parity and all neutral benchmark controls/competence gates pass with zero salience contribution.
    - Online snapshot restores exactly in isolation and rollback succeeds before the switch.
    - Full non-live suite passes under SQLite reads; otherwise evidence is non-pass and legacy ownership is restored.
    - All public summaries are JSON, schema-valid, content-free, digest-bound to private mode-restricted attempts, and leave Phase 1 evidence unchanged.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/storage/test_phase3_evidence.py -k 'authorization_receipt or authorization_summary or import or projection or benchmark or snapshot or restore or rollback or switch or privacy or ci_publication or ci_parser'</automated>
  </verify>
  <done>The authorized data transition is proven end to end, or it fails closed with legacy reads retained and exact evidence of the failed gate.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 03-09-02: Human-publish the exact implementation SHA to an explicitly selected CI ref</name>
  <read_first>
    @.github/workflows/ci.yml
    @tests/test_ci_contract.py
    @tests/storage/test_phase3_evidence.py
    @.planning/evidence/phase-03/read-switch-summary.json
    @aura_backend/storage/cli.py
  </read_first>
  <action>After Task 03-09-01 implementation/evidence commits finish, require a clean worktree and bind the full current `HEAD` SHA. Run the exact synthetic CI-publication verifier, invoke the local `storage.cli prepare-ci-publication`, and use a read-only remote preflight to record the current heads of the two allowed refs; no command may mutate the network. Exclusive-create `/backup/aura-phase-03/phase-03-storage-gate-01/ci/ci-publication-proposal.json` and a private mode-0600 companion. The proposal fixes remote `origin`, preferred ref `refs/heads/phase-03-memory-integrity-ci`, optional `refs/heads/main` only on Ty's explicit normal fast-forward selection, each pre-action remote SHA, exact implementation SHA, workflow `.github/workflows/ci.yml`, its digest at that SHA, allowed human action (`non_force_branch_push` or `workflow_dispatch_existing_exact_ref`), issued time, and finite expiry. Pause for Ty to explicitly select and perform the named action; no task may push or dispatch. After Ty reports the selected ref/action, record that selection and use only read-only remote/GitHub queries through `storage.cli verify-ci-publication` to require that exact remote ref equals the exact SHA and a fresh push/workflow-dispatch run reports that exact `headSha`, `headBranch`, event, workflow path, and workflow digest. For `main`, additionally require its recorded pre-action SHA is an ancestor of the implementation SHA; a missing ancestry proof is non-pass. Exclusive-create private raw verification and public-safe `/backup/aura-phase-03/phase-03-storage-gate-01/ci/ci-publication-receipt.json`. Reject dirty state, different/local-changed SHA, force intent, non-fast-forward `main` rewrite, tag/release/unnamed-ref scope, unrecorded `main` choice, missing/changed selected ref, stale/pre-proposal run, wrong run/SHA/event/workflow/digest, expired proposal, or any task-side network mutation. A queued/in-progress exact run proves publication only; it is not green CI and cannot satisfy final acceptance.</action>
  <what-built>A local content-free proposal bound to the exact clean implementation SHA and a parser that can verify—but never perform—the safe remote publication.</what-built>
  <instructions>Use the exact full SHA shown in `ci-publication-proposal.json`. Preferred: manually run `git push origin EXACT_SHA:refs/heads/phase-03-memory-integrity-ci`, replacing `EXACT_SHA` with that full value and using no force flag. Alternatively, explicitly choose `main` and perform only a normal fast-forward `git push origin EXACT_SHA:refs/heads/main`, or—only if read-only inspection already shows your explicitly selected ref points to the exact SHA—manually run `gh workflow run ci.yml --ref SELECTED_BRANCH`. Report `published phase-03-memory-integrity-ci` or `published main`. Never use `--force`/`--force-with-lease`, non-fast-forward rewrite `main`, create/update a tag or release, publish a different SHA, or dispatch another ref/workflow.</instructions>
  <how-to-verify>1. Confirm the proposal says the worktree was clean and displays the exact full implementation SHA. 2. Prefer `refs/heads/phase-03-memory-integrity-ci`; if choosing `refs/heads/main`, explicitly record that selection and confirm a normal fast-forward only. 3. Confirm the workflow path/digest matches the file at that SHA. 4. Perform exactly one allowed human action. 5. After resuming, confirm the read-only receipt reports the selected ref, same remote SHA, and a fresh matching GitHub run. 6. Treat pending jobs only as publication proof, never Phase 3 success.</how-to-verify>
  <acceptance_criteria>
    - Automated parser/precondition tests pass before the pause and the worktree is clean at the exact bound SHA.
    - Ty alone performs a non-force push to the preferred safe branch, or explicitly selects a normal fast-forward `main` push/dispatch of an already exact ref; no task performs a network mutation.
    - Read-only verification proves the recorded selected ref, fresh run head SHA/branch/event, workflow path, and workflow digest all match the proposal and exact implementation SHA.
    - Explicit `main` selection additionally proves recorded pre-action remote `main` is an ancestor of the implementation SHA.
    - Force/non-fast-forward-main-rewrite/tag/release/unnamed-ref, unrecorded `main`, dirty/different SHA, stale/wrong run, and auto-network cases remain explicit non-pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/storage/test_phase3_evidence.py -k 'ci_publication or ci_parser'</automated>
    <human-check>Ty performs one exact explicitly selected publication action and replies `published phase-03-memory-integrity-ci` or `published main`; read-only verification must then produce a passing receipt.</human-check>
  </verify>
  <resume-signal>After the exact human publication/dispatch, type `published phase-03-memory-integrity-ci` (preferred) or `published main`; otherwise describe the authorization, remote, or workflow issue.</resume-signal>
  <done>The exact clean implementation SHA is present on the explicitly selected remote ref with a fresh matching CI run, proven by public-safe receipt; no required job is yet assumed green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 03-09-03: Collect current-revision CI and accept final behavior</name>
  <read_first>
    @/backup/aura-phase-03/phase-03-storage-gate-01/ci/ci-publication-receipt.json
    @.planning/evidence/phase-03/import-summary.json
    @.planning/evidence/phase-03/benchmark-summary.json
    @.planning/evidence/phase-03/snapshot-restore-summary.json
    @.planning/evidence/phase-03/read-switch-summary.json
    @.planning/evidence/phase-03/migration-authorization-receipt.json
    @.planning/phases/02-provider-and-runtime-core/02-VERIFICATION.md
    @.github/workflows/ci.yml
    @tests/storage/test_phase3_evidence.py
  </read_first>
  <action>Only after Task 03-09-02 produces a passing publication receipt, require the worktree still clean and the current `HEAD` still exactly equals that receipt's implementation SHA. Do not push, dispatch, update a ref, or otherwise mutate the network. Run the full non-live test suite, then invoke `uv run --locked --no-sync python -m aura_backend.storage.cli collect-ci-evidence` in read-only wait/collect mode for the receipt's exact fresh run, workflow `Aura truth lanes`, current workflow digest, and implementation SHA, writing raw private evidence to `/backup/aura-phase-03/phase-03-storage-gate-01/ci/current-revision-ci.private.json` and the public-safe result to `/backup/aura-phase-03/phase-03-storage-gate-01/ci/current-revision-ci.json` without modifying the repository. Wait boundedly for completed-success conclusions from `deterministic-backend`, `lint`, `typing-python`, `typing-frontend`, and `frontend-build`, thereby covering locked clean install, test, lint, typecheck, and build lanes as applicable. Do not require `provider-live-ollama`, Ornith, or another live/model lane. Validate the exact external evidence with `storage.cli validate-evidence`, including publication-receipt digest/run binding. Missing, `not_run`, pending at timeout, old-SHA, failed, cancelled, skipped, resource-limited, wrong-workflow/run, or incomplete required-job evidence is non-pass; unavailable GitHub/credential/network state is recorded as `environment_blocked` and explicitly escalated, never accepted as Phase 3 passed. Historical Phase 2 CI cannot substitute. Do not modify or commit any tracked file between SHA binding and Ty's acceptance; the later documentation-only plan summary must record the proved implementation SHA and cannot claim its own later SHA was tested. Pause only after current-revision CI, the full suite, and all migration evidence pass. Present content-free counts/digests/gate statuses and exact publication/CI SHA/job results. Ty then exercises the loopback UI without exposing private content: send one local turn, verify the exact seven-field response and memory delimiters, search/history scope behavior, stable trace/cursor continuation, restart persistence, and HTTP-200 degraded provider behavior using the documented safe provider-failure probe. If any check fails, record the issue and do not claim Phase 3 accepted; use the proven rollback command if storage ownership is implicated.</action>
  <what-built>Authorized SQLite memory truth, rebuildable Chroma projection, exact recovery/retrieval proof, a fail-closed atomic read-owner transition, and exact current-commit deterministic CI evidence.</what-built>
  <how-to-verify>1. Confirm the authorization receipt is exact, approved, unexpired at consumption, single-use, and bound to all source/restore/plan/private-summary digests. 2. Confirm the publication receipt binds the same clean implementation SHA, explicitly human-selected ref, fresh run, and workflow path/digest, with no force/non-fast-forward-main-rewrite/tag/release/auto-network action. 3. Confirm all four public migration summaries are pass, content-free, and bound to the approved restore. 4. Confirm both historical roots and known anomalies remain represented separately. 5. Confirm salience contribution is exactly zero and every benchmark control ran before competence gates. 6. Confirm snapshot restore and rollback rehearsal pass. 7. Confirm the external CI evidence names that same implementation SHA/current workflow and all five required deterministic jobs completed successfully; historical or live/model jobs do not substitute. 8. Start the loopback app, submit one non-sensitive turn, inspect the exact response shape and bounded search/history behavior, restart, and confirm persistence. 9. Exercise the documented provider-failure probe and confirm HTTP 200 with the same seven response fields. 10. Confirm the read owner is SQLite only if all checks passed.</how-to-verify>
  <acceptance_criteria>
    - Exact evidence verifier and the complete Phase 2 non-live runtime suite pass.
    - The human-action publication receipt and outside-Git CI evidence bind the same clean current implementation SHA, explicitly selected ref, fresh run/current workflow, and completed-success results for all five deterministic required jobs.
    - Any unavailable, missing, pending, old-SHA, failed, cancelled, skipped, `not_run`, resource-limited, or incomplete CI result is environment-blocked/non-pass and cannot reach human acceptance.
    - Human checks confirm the local response, retrieval, restart, degraded-provider, and no-sign-in contracts.
    - Ty accepts the content-free proof or reports a specific defect; no failure is represented as success.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests -m 'not live'</automated>
    <human-check>Ty completes the ten numbered publication/authorization/CI/evidence/local checks and replies `approved`, or describes the failing check.</human-check>
  </verify>
  <resume-signal>Type `approved` only if current-revision CI, every evidence gate, and every local-runtime check passes; otherwise describe the exact failed or environment-blocked check.</resume-signal>
  <done>Phase 3 has explicit human acceptance grounded in current-commit green deterministic CI, passing automated evidence, and the preserved runtime contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Human approval → migration executor | Only the exact authorized plan may license historical reads/import. |
| Durable backup → disposable Chroma copies → SQLite | Personal historical data crosses into the sole truth store without source mutation. |
| SQLite → Chroma projection → retrieval | Rebuildable candidates must remain scoped, provenance-bound, neutral, and non-authoritative. |
| Evidence/private logs → Git-safe summaries | Public proof must be truthful without disclosing conversation content. |
| Legacy owner → SQLite owner → rollback | A single atomic marker controls reads; failure must restore the proven owner. |
| Clean local implementation SHA → human action → safe remote ref/run | Only Ty may publish/dispatch; task-side checks are read-only and exact-SHA/workflow bound. |
| Current Git HEAD/GitHub CI → final human acceptance | Only exact-current deterministic green jobs may support Phase 3 acceptance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-09-01 | Spoofing / Repudiation | authorization/evidence chain | mitigate | Require immutable exact-scope/result/expiry receipt, private-summary/source/restore/plan digests, and single-use ID; adversarial verifier. |
| T-03-09-02 | Tampering | migration/import | mitigate | Disposable-only opens, source before/after hashes, idempotency identities, exact counts/table digests, no mutation. |
| T-03-09-03 | Information Disclosure | public summaries/traces | mitigate | JSON schema allowlists, opaque IDs/HMACed fixture values, private mode-0600 logs outside Git. |
| T-03-09-04 | Elevation / Information Disclosure | retrieval | mitigate | Hard scope/provenance/supersession/staleness gates before RRF; neutral candidates and zero salience. |
| T-03-09-05 | Denial of Service | benchmark/snapshot/switch | mitigate | Predeclared bounded caps/timeouts, explicit resource-limit non-pass, legacy owner retained or restored. |
| T-03-09-06 | Tampering / Repudiation | read-owner marker | mitigate | Atomic replace, gate digest bundle, rollback rehearsal, post-switch full-suite gate. |
| T-03-09-07 | Spoofing / Repudiation | current-revision CI | mitigate | Clean exact HEAD/current workflow binding, five completed-success jobs, negative parser cases, external evidence so proof does not change SHA. |
| T-03-09-08 | Tampering / Elevation | remote CI publication | mitigate | Blocking human action, preferred safe branch or explicit fast-forward main selection, no force/rewrite/tag/release, exact remote/run SHA/workflow proof, no task network mutation. |
| T-03-09-SC | Tampering | package supply chain | mitigate | Locked existing environment only; no package install, sync, manifest, lockfile, or dependency change. |
</threat_model>

<verification>
Run the exact task verifiers, then `uv run --locked --no-sync python -m pytest -q tests -m 'not live'`. Compare behavior with the Phase 2 runtime suite and validate all Phase 3 summaries plus the authorization receipt. Require Ty's human-action publication receipt proving the exact clean implementation SHA on the explicitly selected `origin` ref (preferred `phase-03-memory-integrity-ci`, or `main` only by explicit normal fast-forward choice) with a fresh exact-head/workflow run, followed by outside-Git CI evidence for that same SHA/run/current workflow with completed-success `deterministic-backend`, `lint`, `typing-python`, `typing-frontend`, and `frontend-build`. No task pushes or dispatches. Historical CI and live/model lanes cannot substitute. The plan passes only with exact publication, current-revision CI, and recorded human acceptance; any failed/environment-blocked gate leaves or restores legacy reads and produces a non-pass result.
</verification>

<source_coverage>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Coverage | Status |
|---|---|---|---|---|
| GOAL | — | Trustworthy durable, recoverable, deletable, exportable, private local memory | Final migration/recovery/runtime proof | COVERED |
| REQ | PRES-04 | Private artifacts stay out of Git | Public/private summaries plus hash invariance | COVERED |
| REQ | TEST-03 | Persistence/retrieval/deletion failure tests | Receipt/publication/CI adversarial parsers, evidence verifier, full non-live suite, current CI | COVERED |
| REQ | DATA-01 | One owner across persistence, index, backup/restore, export, retention, and deletion | Imported sole ledger and read owner plus full lifecycle suite | COVERED |
| REQ | DATA-02 | Consistent snapshot and exact restore | Online snapshot/isolated restore | COVERED |
| REQ | DATA-03 | Every advertised export is actual data; unsupported formats reject | Preserved lifecycle/full-suite gate | COVERED |
| REQ | DATA-04 | Deletion enumerates and verifies all affected copies | Preserved lifecycle/full-suite gate | COVERED |
| REQ | DATA-05 | Retrieval pagination, relevance, latency, and duplicates are bounded/measured | Frozen benchmark plus human runtime check | COVERED |
| REQ | DATA-06 | Events and derived memories retain provenance and supersession | Import/integrity gates | COVERED |
| REQ | DATA-09 | Affect-neutral eligible set precedes any salience seam | Benchmark hard gates, trace, and zero salience | COVERED |
| CONTEXT | D-01 | SQLite sole truth / atomic idempotence | Import, owner, and full-suite gates | COVERED |
| CONTEXT | D-02 | Immutable events and provenance | Exact anomaly-preserving import | COVERED |
| CONTEXT | D-03 | Neutral FTS/vector, hard gates, RRF, traces/cursors, salience zero | Projection and benchmark proof | COVERED |
| CONTEXT | D-04 | Both roots and copy-only Memvid; no source mutation | Disposable-only reads and hash checks | COVERED |
| CONTEXT | D-05 | SQLite backup and exact isolated restore | Snapshot summary | COVERED |
| CONTEXT | D-06 | Truthful JSON-only export | Preserved lifecycle/full-suite gate | COVERED |
| CONTEXT | D-07 | Explicit deletion and residual reporting | Preserved lifecycle/full-suite gate | COVERED |
| CONTEXT | D-08 | Sanitized controls before optimization | Frozen benchmark executes controls first | COVERED |
| CONTEXT | D-09 | Loopback/no auth and exact compatibility | Full suite plus human local check | COVERED |
| RESEARCH | Real-data migration safety | Immutable receipt, disposable imports, parity, rollback, human-bound publication, current-revision CI | COVERED |

Deferred/out-of-phase graph/RLM, affect salience, frontend redesign, auth, encryption claim, repair/deletion, broad dependency, and Git-history work are excluded rather than gaps.
</source_coverage>

<success_criteria>
- Authorized disposable copies import exactly into SQLite with provenance and anomalies intact; retained inputs remain unchanged.
- Chroma rebuilds from SQLite and neutral hybrid retrieval passes controls, competence, stable trace/cursor, and salience-zero gates.
- SQLite snapshot restores exactly in isolation; rollback and post-switch compatibility are proven.
- SQLite becomes read owner only on an all-green result; every failure remains truthful and fail-closed.
- Ty alone publishes the exact clean implementation SHA to the preferred safe branch or an explicitly selected normal fast-forward `main` ref; read-only proof rejects every unsafe/different/stale action.
- The exact current implementation commit has green deterministic test/lint/typecheck/build CI; unavailable or non-green evidence remains explicitly non-pass/environment-blocked.
- Complete Phase 2 runtime behavior and human local verification pass without auth, frontend redesign, or private evidence leakage.
</success_criteria>

<output>
Create `.planning/phases/03-memory-integrity-and-data-lifecycle/03-09-SUMMARY.md` when done.
</output>
